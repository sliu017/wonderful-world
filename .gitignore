# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
/scripts/venv

# Internal data-review tool. Tracked (not shipped — Vite only bundles src/) so
# its hand-curated output, manual-review/reviews.json, is backed up & versioned.
# Only the atomic-write scratch file is ignored:
/manual-review/reviews.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
